{
  "name": "klaxo-blog",
  "private": true,
  "description": "Klaxo Travel Tips - static blog. Run `npm run build:css` after changing Tailwind classes in the HTML.",
  "scripts": {
    "build:css": "tailwindcss -i src/tailwind.css -o css/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.9",
    "tailwindcss": "^3.4.17"
  }
}
